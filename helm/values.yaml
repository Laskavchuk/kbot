replicaCount: 1
image:
  repository: "ghcr.io/laskavchuk/kbot/helm"
  tag: "v1.0.3-bab286b"
  arch: "amd64"
  pullPolicy: "IfNotPresent"
imagePullSecrets:
  - name: ghcr-secret # секрет для GHCR
secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
securityContext:
  privileged: true
